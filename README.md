# efficient-tokens
A plugin to use your context window more efficiently. Currently comes in the form of an agent hook

## Usage

```bash
/plugin marketplace add yairyairyair/efficient-tokens
/plugin install efficient-tokens@yairyairyair
```
